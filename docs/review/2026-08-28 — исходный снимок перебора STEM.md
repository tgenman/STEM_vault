---
created: 2026-08-28
status: baseline
temporary: true
---
# исходный снимок перебора STEM — 2026-08-28

Снимок зафиксирован 2026-08-28 в 09:48:57 MSK до создания этого файла. Он задаёт исходный состав полного перебора, но ничего не объявляет корректным, актуальным или уже просмотренным. Термины определены в [CONTEXT.md](../../CONTEXT.md).

## идентичность tracked-состояния

- child commit: `2092fe755b76d9e235b3c26fcfa90283b35e390e`;
- branch: `main`;
- upstream: `origin/main`;
- divergence: `0 ahead / 0 behind`;
- worktree: clean;
- полный manifest 1 925 tracked-файлов задаётся Git tree этого commit, поэтому отдельная копия списка tracked-путей не создаётся.

Этот baseline и последующие временные отчёты в исходные 1 925 файлов не входят.

## состав tracked-состояния

| слой | количество |
| --- | ---: |
| все tracked-файлы | 1 925 |
| Markdown | 1 455 |
| человеческий корпус: `notes/**/*.md` | 1 400 |
| человеческий корпус: `diploma_references/*.md` | 33 |
| служебный Markdown | 22 |
| Cursor rules `.mdc` | 4 |
| attachments | 460 |
| templates | 9 |
| `docs/**` | 3 |
| `meta/**` | 4 |

Расширения tracked-файлов: 1 455 `.md`, 4 `.mdc`, 426 `.png`, 10 `.jpeg`, 18 `.svg`, 3 `.pdf`, 3 `.jpg`, 3 `.css`, 1 `.js` и 2 файла без расширения.

## ignored runtime-состояние

На момент снимка существовало 50 ignored-файлов. Они входят в учёт всего vault, но рассматриваются по роли: значимая конфигурация проверяется функционально; workspace, кеши и vendor-сборки исключаются по роли. Точные hashes 49 стабильных ignored-файлов записаны в [приложении к baseline](2026-08-28%20%E2%80%94%20ignored-runtime.sha256). `.obsidian/workspace.json` учтён как пятидесятый volatile runtime-файл без контрольного hash: он изменился во время проверки снимка.

Всего на диске без `.git`: 1 975 файлов и 222 MB.

## живой граф Obsidian

Корневой `zettle_life` на момент снимка индексировал:

- 1 920 файлов внутри `STEM_vault/`;
- все 1 455 Markdown-файлов;
- 260 Markdown-файлов хотя бы с одной unresolved-ссылкой;
- 645 различных unresolved-целей и 676 occurrences.

Эти значения — диагностические сигналы. Они не являются очередью исправлений и не разрешают удаление, создание или перелинковку заметок.

## граница охвата

- человеческий текст просматривается содержательно;
- инструкции, templates, скрипты и конфигурация — функционально;
- attachments — по существованию, использованию, дублям и происхождению;
- значимое ignored-состояние `.obsidian` — как runtime-конфигурация;
- `.git`, кеши и vendor-сборки учитываются, но исключаются по роли без построчного ревью.

Любой объект, добавленный после этого снимка, относится к delta. Изменение объекта после его просмотра требует отдельного решения о повторном просмотре.
